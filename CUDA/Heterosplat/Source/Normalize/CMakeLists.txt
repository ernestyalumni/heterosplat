ADD_LIBRARY(Normalize
  Convention.cpp
  Transform.cu
  )

TARGET_INCLUDE_DIRECTORIES(Normalize PUBLIC ${CMAKE_SOURCE_DIR})

SET_TARGET_PROPERTIES(Normalize
  PROPERTIES
  CUDA_SEPARABLE_COMPILATION ON)
